name: Demo

on:
  push:
    branches: [ main ]
#   schedule:
#     - cron: "0 0 * * *"
  workflow_dispatch:
#   watch:
#     types: [started]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3
      
      - uses: narze/append_on_merge@main
